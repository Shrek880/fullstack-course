```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User submits form

    Note right of browser: JS prevents default form submission

    browser->>browser: Create note object
    browser->>browser: Add note to list
    browser->>browser: Re-render notes list

    browser->>server: POST /new_note_spa (JSON)
    activate server
    Note right of server: Server saves note

    server-->>browser: 201 Created
    deactivate server

    Note right of browser: No page reload
